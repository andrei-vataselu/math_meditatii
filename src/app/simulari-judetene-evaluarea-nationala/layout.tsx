import metadata from "./page.metadata";

export { metadata };

export default function SimulariJudeteneEvaluareaNationalaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
